Methods i should be familiar with

Array
    - Some - returns boolean based on the filter i assign to it. Great for comparing two arrays.
    

String
    - charCodeAt(i)
    - For string to integer conversion, use
        - parseInt for integer math 
        - Number (number.isInteger())to check if is a number
        - !isNaN (checks if it is not a number)
        - isfinite

